# .github/workflows/main.yml

name: Angular CI/CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm install
      - name: Run tests
        run: npx ng test --no-watch --browsers=ChromeHeadless
      - name: Build application
        run: npx ng build
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: production-build
          path: dist/sjp-locacoes-frontend/

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: production-build

      - name: Debug Secrets
        run: |
          echo "Site ID starts with: ${{ env.SITE_ID_PREFIX }}"
          echo "Auth Token starts with: ${{ env.AUTH_TOKEN_PREFIX }}"
        env:
          SITE_ID_PREFIX: ${{ substring(secrets.NETLIFY_SITE_ID, 0, 5) }}
          AUTH_TOKEN_PREFIX: ${{ substring(secrets.NETLIFY_AUTH_TOKEN, 0, 5) }}

      - name: Deploy to Netlify
        uses: nwtgck/actions-netlify@v3.0
        with:
          publish-dir: '.'
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}
          NETLIFY_CLI_VERSION: "17"